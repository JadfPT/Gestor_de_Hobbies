<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.PasswordField?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Pane?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.StackPane?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="ui.controllers.LoginController"
            stylesheets="@../style/mainstyle.css">

<center>
    <StackPane>
        <HBox styleClass="auth-card" spacing="0" StackPane.alignment="CENTER">
            <VBox spacing="16" styleClass="auth-left">
                <Label text="Gestor de Hobbies" styleClass="auth-app-title"/>

                <VBox spacing="4">
                    <Label text="Bem-vindo de volta 👋" styleClass="auth-title"/>
                    <Label text="Inicia sessão na tua conta local para veres os teus hobbies e sessões."
                           wrapText="true"
                           styleClass="auth-subtitle"/>
                </VBox>

                <VBox spacing="10">
                    <VBox spacing="4">
                        <Label text="Nome de utilizador" styleClass="field-label"/>
                        <TextField fx:id="txtUsername"
                                   promptText="ex: joao"
                                   styleClass="field-input"/>
                    </VBox>

                    <VBox spacing="4">
                        <Label text="Password" styleClass="field-label"/>
                        <PasswordField fx:id="txtPassword"
                                       promptText="••••••••"
                                       styleClass="field-input"/>
                    </VBox>
                </VBox>

                <HBox spacing="10">
                    <Button text="Entrar"
                            styleClass="primary-button"
                            onAction="#onLogin"/>
                    <Button text="Criar conta"
                            styleClass="secondary-button"
                            onAction="#onOpenRegister"/>
                </HBox>

                <Label fx:id="lblMensagem"
                       styleClass="auth-message"
                       wrapText="true"/>
            </VBox>

            <VBox styleClass="auth-right" spacing="10">
                <Label text="T02 — Os meus passatempos"
                       styleClass="auth-right-title"/>
                <Label text="Regista as tuas leituras, gaming, filmes, música e muito mais. Acompanha o teu progresso ao longo do tempo."
                       wrapText="true"
                       styleClass="auth-right-text"/>
                <Region VBox.vgrow="ALWAYS"/>
                <Label text="Trabalho Prático POO 2025/2026 · UBI"
                       styleClass="auth-right-footer"/>
            </VBox>

        </HBox>
    </StackPane>
</center>

</BorderPane>
