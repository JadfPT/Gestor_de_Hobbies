<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ScrollPane?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Pane?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="ui.controllers.MainController"
            stylesheets="@../style/mainstyle.css">

    <top>
        <HBox spacing="10" styleClass="top-bar">
            <Label text="Gestor de Hobbies" styleClass="app-title"/>
            <Pane HBox.hgrow="ALWAYS"/>
            <Label text="T02 — Os meus passatempos" styleClass="app-subtitle"/>
        </HBox>
    </top>

    <left>
        <VBox spacing="10" styleClass="sidebar">
            <Label text="Menu" styleClass="sidebar-title"/>

                <Button text="🎯  Hobbies"
                    styleClass="sidebar-button"
                    onAction="#onHobbiesClicked"/>

                <Button text="📅  Sessões"
                    styleClass="sidebar-button"
                    onAction="#onSessionsClicked"/>

            <Button text="📊  Estatísticas"
                    styleClass="sidebar-button"
                    onAction="#onStatsClicked"/>

                <Button text="⚙  Definições"
                    styleClass="sidebar-button"
                    onAction="#onSettingsClicked"/>

            <Region VBox.vgrow="ALWAYS"/>
        </VBox>
    </left>

    <center>
        <ScrollPane fx:id="contentScroll"
                    fitToWidth="true"
                    pannable="true"
                    hbarPolicy="AS_NEEDED"
                    vbarPolicy="AS_NEEDED"
                    styleClass="content-scroll">

            <content>
                <StackPane fx:id="contentArea" styleClass="content-area">
                          <VBox alignment="CENTER" spacing="20" styleClass="welcome-card">
                           <Label text="Bem-vindo ao Gestor de Hobbies" styleClass="hero-title"/>

                           <Label fx:id="lblStatus"
                               text="Organiza hobbies, regista sessões e acompanha progresso."
                               wrapText="true"
                               styleClass="hero-subtitle"/>

                           <Button text="Começar a adicionar hobbies"
                                styleClass="primary-button"
                                onAction="#onHobbiesClicked"/>
                          </VBox>
                </StackPane>
            </content>
        </ScrollPane>
    </center>

    <bottom>
        <HBox styleClass="status-bar">
            <Pane HBox.hgrow="ALWAYS"/>
            <Label text="Trabalho Prático POO 2025/2026 · UBI"/>
        </HBox>
    </bottom>

</BorderPane>