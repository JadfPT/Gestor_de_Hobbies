<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Pane?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="ui.controllers.MainController"
            stylesheets="@../style/mainstyle.css">

    <!-- TOP BAR -->
    <top>
        <HBox spacing="10" styleClass="top-bar">
            <Label text="Gestor de Hobbies" styleClass="app-title"/>
            <Pane HBox.hgrow="ALWAYS"/>
            <Label text="T02 — Os meus passatempos" styleClass="app-subtitle"/>
        </HBox>
    </top>

    <!-- SIDE MENU -->
    <left>
        <VBox spacing="10" styleClass="sidebar">
            <Label text="Menu" styleClass="sidebar-title"/>

            <Button text="🏷️  Hobbies"
                    styleClass="sidebar-button selected"
                    onAction="#onHobbiesClicked"/>
            <Button text="📅  Sessões"
                    styleClass="sidebar-button"
                    onAction="#onSessoesClicked"/>
            <Button text="📊  Estatísticas"
                    styleClass="sidebar-button"/>
            <Button text="⚙️  Definições"
                    styleClass="sidebar-button"/>

            <Region VBox.vgrow="ALWAYS"/>
            <Label text="v0.1 · Draft" styleClass="sidebar-footer"/>
        </VBox>
    </left>

    <!-- MAIN CONTENT (AQUI TROCAMOS AS VIEWS) -->
    <center>
        <StackPane fx:id="contentArea" styleClass="content-area">
            <VBox alignment="CENTER" spacing="15" styleClass="welcome-card">
                <Label text="Bem-vindo ao Gestor de Hobbies"
                       styleClass="card-title"/>

                <Label fx:id="lblStatus"
                       text="Bem-vindo ao Gestor de Hobbies!"
                       wrapText="true"
                       styleClass="card-text"/>

                <Button text="Começar a adicionar hobbies"
                        styleClass="primary-button"
                        onAction="#onHobbiesClicked"/>
            </VBox>
        </StackPane>
    </center>

    <!-- STATUS BAR -->
    <bottom>
        <HBox styleClass="status-bar">
            <Pane HBox.hgrow="ALWAYS"/>
            <Label text="Trabalho Prático POO 2025/2026 · UBI"/>
        </HBox>
    </bottom>

</BorderPane>
